{
  "name": "vag-demo-gateway",
  "version": "0.1.0",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "description": "Local bounded demo gateway for VAG public demo. No external calls, no cloud, no shell.",
  "scripts": {
    "start": "node src/server.mjs",
    "test": "node --test src/*.test.mjs"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
